# Mac OS vs. Linux flavors like Ubuntu 

### for MAC system 

$ uname -a

If the output starts with **Darwin, you are running macOS, not Linux!**

SR Note: 
 - so we are runnig  MAC Darwin Unix , not  a Linux ...
 - Reason: when using CodeX, we have issues when using 'OrbStack Ubuntu' server 
 - CodeX has issues with OrbStack Ubuntu when running it's internal Tests etc.. 
 - that is the reason SREDDY sticked with 'Mac Darwin' default OS
 -
 - **Notes on Wez Term:**
 - even though we are using WezTerm , it is still 'Mac Darwin OS', not Linux becasue we did not start 'orb' command AFTER opeinng 'Wez Term'
 - so at least WeZterm UI is good, go with it instead of MAC Terminal .